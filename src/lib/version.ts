// Bumped by 0.001 on every push so the running deployment's version can be
// visually compared against what was actually pushed, without digging
// through commit hashes.
export const APP_VERSION = 'v0.065'
